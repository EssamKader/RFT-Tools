# Isolated Footing — Handover (2026-09-24)

## State at handover
- Merged & closed: #198, #199, #200, #201, #202, #203, #204, plus #204's R4/R5 follow-up (PR #219).
- Only open issue under `element:footing`: **#205** — "Wire Footing UI (single window, dialogs, persistence, error handling)". Unblocked (its blockers #198-#204 are all merged). Labeled `needs-triage` — not yet triaged/started.
- Latest commits on `master`: `fac8ec1` (R4/R5, #219), `7bd1263` (#204/#218), `c1bb8d3` (#203/#217), `7043394` (#202/#216).
- No open worktrees. Working tree has one pre-existing unrelated modification: `.claude/agents/implementer.md` (not part of footing work — check before touching).

## Rulings on record (docs/footing/spec-amendments.md)
- R1-R3: from earlier sessions (top-mat vertical convention, etc.) — see file directly.
- **R4**: `perimeter_tie`'s first level starts 250mm above the top of the bottom mesh (same datum as `footing_dowels`'s bend corner: `bottom_cover_mm + mesh_bar_x_dia_mm + mesh_bar_y_dia_mm`).
- **R5**: the two-bar splice cut-length split is a direct two-field engineer input (first bar length + second bar length), validated only for summing to the total — never derived by formula.

## Core module map (RFT.lib/rft/core/)
- `footing_plan.py` — the ONE composing module. `build_footing_plan(inputs)` is what every consumer (UI, Revit adapter) should read from. `FootingInputs` is append-only; currently ends with `perimeter_tie_first_bar_length_mm`, `perimeter_tie_second_bar_length_mm` (13 trailing defaults, all `None`).
- `footing_mesh.py` — bottom mat done, top mat math done (#201) but nothing places it.
- `footing_dowels.py` — single representative dowel bar + embedment, Revit adapter exists (`rft.revit.footing_dowels`).
- `footing_dowel_tie.py` (#203) — vertical ladder/spacing math only; loop shape blocked (see below).
- `footing_perimeter_tie.py` (#204 + R4/R5) — full geometry, splice, vertical ladder, bar-length split. No Revit placement adapter yet.

## What's left before the element is "done"
1. **#205 — wire the UI.** Ready to triage/start now. Scope: one modeless window exposing every input built so far (mesh, dowels, dowel-tie, perimeter-tie incl. the new R4/R5 fields), wired to `footing_plan.build_footing_plan` for preview + a single Place action. Reuses RFT.lib's existing dialog/persistence/error-handling infra as-is. Does NOT add new placement logic — UI wiring only.
2. **Top mesh placement adapter** — math done (#201), nothing draws it in Revit; only bottom mat gets placed today.
3. **Hook wiring** — hook decision (#199) is computed but not turned into an actual `RebarHookType`/orientation on a placed bar.
4. **Perimeter tie Revit placement adapter** — R4/R5 give full Z-elevations/lengths, but the closed-loop-on-footing-host case is unverified against a live host. When built: reuse `column_place_ties`'s `norm = XYZ.BasisZ` convention (horizontal loop plane) — NOT `footing_dowels`'s `XYZ.BasisY` (that was for a bent bar in a vertical plane). Don't conflate the two norm conventions again (this exact mistake was caught once already in #202 review).
5. **Dowel bar array** — the two blocking decisions are now RULED (R6/R7 in
   `docs/footing/spec-amendments.md`, 2026-09-24), not yet implemented:
   - dowel count/layout mirrors ColumnRFT's own longitudinal bar layout
     (bar count per face + corner bars) — R6.
   - the column's second (depth) cross-section dimension is sourced live
     from the column element by reusing `rft.revit.column_host.
     read_section_mm`/`read_orientation` as-is, with the column itself
     auto-detected above the footing via a `ReferenceIntersector` ray-cast
     (new direction, needs its own tracer-bullet verification), refusing
     with "no column is attached to this footing" if none is found — no
     manual fallback — R7.
   - Not yet ticketed/started: this still needs its own To-Spec/To-Tickets
     pass before an implementer picks it up.
6. **`dowel_tie`'s closed-loop shape + placement** — was blocked on the
   same two items as #5; those are now ruled (see #5), so this can be
   ticketed once #5's implementation lands.
7. **Live-host verification** — most core math has never run against a real Revit model. Only #197's own tracer bullet (one straight bar hosted on a footing) has live-host proof. Everything else (bent dowel, both tie types, top mesh) is unit-tested only.

Explicitly OUT of scope (decided, not gaps): multi-footing/batch placement (spec §0 excludes it); rotated footings (refused, not supported).

## Next actionable step
Triage and start **#205** (`gh issue edit 205 --add-label ready-for-agent --remove-label needs-triage`), create worktree `.claude/worktrees/footing-205`, delegate to `footing-implementer` per the established pipeline. Confirm with Essam first — his last message was a status question, not yet an instruction to start #205.

## Standing process rules (carry into new session)
- Read `CONTEXT.md` / `REUSE_GUIDELINES.md` first.
- `dowel_tie`/`perimeter_tie` reuse `ColumnRFT`'s tie/stirrup geometry where applicable — decided, don't re-litigate.
- Add `element:footing` label to new issues.
- Follow spec's own §11 tracer-bullet ticket order.
- Only write tests for genuinely new math — not for logic inherited from RFT.lib/ColumnRFT.
- Pipeline per ticket: triage → worktree → delegate to `footing-implementer` subagent → PR → `/code-review` on the PR → fix confirmed findings → report to Essam → wait for merge → close issue → remove worktree.
- Explicit-Refusals pattern for spec gaps: don't guess, surface to Essam in plain language, record his ruling as a new numbered entry (R6, R7, ...) in `docs/footing/spec-amendments.md`.
